"""Gateway adapters. Mattermost (mattermostautodriver) first; others may follow.

Each gateway normalizes its platform events into neutral message/action types and
routes them to the agent runtime.
"""
